[pytest]
# Scope collection to our own suite. benchmarks/estate holds cloned third-party
# repos (with their own tests and deps) used as analysis fixtures, not our tests.
testpaths = tests
norecursedirs = benchmarks .factory* .git fixtures scripts docs *.egg-info
addopts = -q --import-mode=importlib
pythonpath = src
